# 🔒 The footer is locked

`src/components/site/Footer.tsx` is shared by **every** project in the workshop cohort so the
whole set of sites stays consistent. 

**Rules:**
- Do not edit, restyle, or replace the footer.
- Do not remove it from `src/app/layout.tsx` (it renders on every page).
- If a requirement seems to need a footer change, that's a facilitator decision — flag it, don't do it.

Everything *above* the footer is yours to build.
