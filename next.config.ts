import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Students rarely need to touch this. Add config here only when a feature requires it.
  reactStrictMode: true,
};

export default nextConfig;
